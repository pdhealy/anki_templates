find . -type f -name "._*" -delete

---

Overwrite the existing Chinese Character-to-Zhuyin translation method observed in file `workspace/anki_templates/src/chinese_vocab (小牛津)/zhuyin-translation/convert.js` with the new working proof-of-concept implementation observed in file `workspace/anki_templates/src/chinese_vocab (小牛津)/zhuyin-translation/convert.js`. Use the Context7 MCP server where required to pull documentation for Anki. The desired result it that the Zhuyin translations should appear vertically to the right hand side of each character in `class=mainCharacter>{{Front}}`

---

Test all changes to ensure completion against request and target outcome. Install all required tools and dependencies. Install all required CLI or headless tools to complete task.